<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApp" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/splash_day</item>
        <item name="colorPrimaryVariant">@color/splash_morning</item>
        <item name="colorOnPrimary">@color/splash_evening</item>

        <!-- Secondary brand color. -->
        <item name="android:forceDarkAllowed">false</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>
</resources>